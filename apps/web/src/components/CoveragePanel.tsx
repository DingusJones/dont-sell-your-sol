import type {PortfolioEnvelope} from '../../../../packages/domain/src/types.ts';
export function CoveragePanel({snapshot,onRetry}:{snapshot:PortfolioEnvelope|null;onRetry:()=>void}){return <aside className="coverage-panel"><div className="section-title"><h2>Coverage, explained</h2><span aria-hidden="true">◈</span></div><p className="muted small">A successful balance check does not prove every position was found.</p><div className="coverage-count"><strong>{snapshot?.completeness.checked.length??0}</strong><span>of {snapshot?.completeness.expected.length??'—'} named checks complete</span></div><div className="provider-list">{snapshot?.providers.map(p=><div key={p.capability} className="provider"><div><span>{p.capability}</span><span className={`badge ${['success','empty'].includes(p.state)?'good':'warning'}`}>{p.state}</span></div><p className="small muted">{p.message}</p></div>)}</div><button onClick={onRetry}>Retry checks</button><p className="small muted">Manifest {snapshot?.completeness.manifestVersion??'2026-09-06.1'}<br/>Prices, native staking, protocol decoding and exact exits require independent verification.</p></aside>;}
