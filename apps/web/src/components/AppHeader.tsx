import {shortAddress} from '../../../../packages/domain/src/wallet.ts';
export function AppHeader({address,demo,onRemove,onChange,onRefresh,refreshing}:{address:string|null;demo:boolean;onRemove:()=>void;onChange:()=>void;onRefresh:()=>void;refreshing:boolean}){
 return <header className="app-header"><a className="brand" href="#" onClick={e=>{e.preventDefault();onRemove();}} aria-label="Dont sell your Sol home"><span className="brand-mark" aria-hidden="true">≋</span><span>Dont sell<br/><strong>your Sol<span className="accent">.</span></strong></span></a><div className="header-right"><span className="network"><span className="dot"/> Solana mainnet</span>{address&&<><button onClick={onChange} title={address} className="wallet-pill">{demo?'Demo wallet':shortAddress(address)} <span aria-hidden="true">⌄</span></button><button onClick={onRefresh} disabled={refreshing} aria-label="Refresh portfolio">{refreshing?'Checking…':'↻ Refresh'}</button><button onClick={onRemove} className="text-button">Remove</button></>}</div></header>;
}
